name: PLAN1152 temporary profile photo runner

on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/plan1152-runner.yml
      - scripts/plan1152-profile-photo-controls.cjs

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout current main
        uses: actions/checkout@v5
        with:
          ref: main
          fetch-depth: 0
      - name: Fetch patch script
        run: git show origin/tmp/plan1152-runner:scripts/plan1152-profile-photo-controls.cjs > /tmp/plan1152-profile-photo-controls.cjs
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: pnpm
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Apply patch
        id: apply
        continue-on-error: true
        run: node /tmp/plan1152-profile-photo-controls.cjs > /tmp/plan1152-patch.log 2>&1
      - name: Upload patch diagnostics
        if: steps.apply.outcome == 'failure'
        uses: actions/upload-artifact@v4
        with:
          name: plan1152-patch-diagnostics
          path: /tmp/plan1152-patch.log
      - name: Stop on patch failure
        if: steps.apply.outcome == 'failure'
        run: |
          cat /tmp/plan1152-patch.log
          exit 1
      - name: Test
        run: pnpm run test
      - name: Typecheck
        run: pnpm run typecheck
      - name: Lint
        run: pnpm run lint
      - name: Build
        run: pnpm run build
      - name: Push verified feature branch
        run: |
          git config user.name "GO IRL PLAN1152 Runner"
          git config user.email "goirl-plan1152@users.noreply.github.com"
          git checkout -b fix/profile-photo-controls
          git add src/App.tsx src/styles.css api/_shared/telegram-share-card-image.ts api/_shared/telegram-share-card-svg.ts api/_shared/telegram-share-card-svg.test.ts
          git commit -m "fix: simplify profile photo controls"
          git push origin HEAD:refs/heads/fix/profile-photo-controls
