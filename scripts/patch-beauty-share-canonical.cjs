const fs = require("node:fs");
const path = require("node:path");
const root = process.cwd();
const read = (file) => fs.readFileSync(path.join(root, file), "utf8");
const write = (file, content) => {
  const target = path.join(root, file);
  fs.mkdirSync(path.dirname(target), { recursive: true });
  fs.writeFileSync(target, content, "utf8");
};
const replaceOne = (file, before, after) => {
  const current = read(file);
  const first = current.indexOf(before);
  if (first < 0) throw new Error(`PATCH_TARGET_NOT_FOUND:${file}:${before.slice(0, 100)}`);
  if (current.indexOf(before, first + before.length) >= 0) throw new Error(`PATCH_TARGET_NOT_UNIQUE:${file}:${before.slice(0, 100)}`);
  write(file, current.slice(0, first) + after + current.slice(first + before.length));
};
const replacements = [["src/cardShare.ts","export const buildCardShareText = ({ title, date, address, url }: CardShareContent) =>\n  [[`GO IRL: ${title}`, date, address].filter(Boolean).join(\"\\n\"), url].filter(Boolean).join(\"\\n\\n\");\n","export const buildCardShareText = ({ title, date, address, url }: CardShareContent) =>\n  [[`GO IRL: ${title}`, date, address].filter(Boolean).join(\"\\n\"), url].filter(Boolean).join(\"\\n\\n\");\n\nconst beautyShareSlugFromUrl = (value: string) => {\n  try {\n    const url = new URL(value);\n    const match = url.pathname.match(/^\\/beauty\\/([^/]+)\\/?$/i);\n    const slug = match?.[1] ? decodeURIComponent(match[1]).trim().toLowerCase() : \"\";\n    return beautySlugPattern.test(slug) ? slug : \"\";\n  } catch {\n    return \"\";\n  }\n};\n\nexport const isBeautyCardShareContent = (content: CardShareContent) =>\n  Boolean(beautyShareSlugFromUrl(content.url));\n"],["src/cardShare.ts","    const beautyMatch = inviteUrl.pathname.match(/^\\/beauty\\/([^/]+)\\/?$/i);\n    const beautySlug = beautyMatch?.[1] ? decodeURIComponent(beautyMatch[1]).trim().toLowerCase() : \"\";\n    if (beautySlugPattern.test(beautySlug)) {\n      const previewUrl = new URL(\"/api/meta/event-preview\", fallbackOrigin);\n      previewUrl.searchParams.set(\"slug\", beautySlug);\n      previewUrl.searchParams.set(\"language\", content.language || \"ru\");\n      if (content.date.trim()) previewUrl.searchParams.set(\"date\", content.date.trim());\n      return previewUrl.toString();\n    }\n","    const beautySlug = beautyShareSlugFromUrl(inviteUrl.toString());\n    if (beautySlug) {\n      const previewUrl = new URL(\"/api/meta/event-preview\", fallbackOrigin);\n      previewUrl.searchParams.set(\"slug\", beautySlug);\n      previewUrl.searchParams.set(\"language\", content.language || \"ru\");\n      if (content.date.trim()) previewUrl.searchParams.set(\"date\", content.date.trim());\n      previewUrl.searchParams.set(\"v\", \"10\");\n      return previewUrl.toString();\n    }\n"],["src/cardShare.ts","  if (channel === \"whatsapp\") {\n    const landingUrl = buildCardShareLandingUrl(normalizedContent);\n    const message = buildCardShareText({ ...normalizedContent, url: landingUrl });\n    return `https://wa.me/?text=${encodeURIComponent(message)}`;\n  }\n","  if (channel === \"whatsapp\") {\n    if (isBeautyCardShareContent(normalizedContent)) {\n      return `https://wa.me/?text=${encodeURIComponent(buildMetaEventPreviewUrl(normalizedContent))}`;\n    }\n    const landingUrl = buildCardShareLandingUrl(normalizedContent);\n    const message = buildCardShareText({ ...normalizedContent, url: landingUrl });\n    return `https://wa.me/?text=${encodeURIComponent(message)}`;\n  }\n"],["src/components/CardShareAction.tsx","                if (channel.id === \"whatsapp\") {\n                  void prepareWhatsAppCard();\n                } else {\n                  void share(channel.id);\n                }\n","                if (channel.id === \"whatsapp\") {\n                  if (canPrepareBeautyTelegramShare(url)) {\n                    setOpen(false);\n                    setExpanded(false);\n                    openExternalShareTarget(buildCardShareTarget(\"whatsapp\", content));\n                  } else {\n                    void prepareWhatsAppCard();\n                  }\n                } else {\n                  void share(channel.id);\n                }\n"],["api/_shared/telegram-event-card.ts","  language: \"ru\" | \"uk\" | \"cs\" | \"en\";\n};\n","  language: \"ru\" | \"uk\" | \"cs\" | \"en\";\n  beautyServices?: Array<{ name: string; priceCzk: number }>;\n  publicProfileUrl?: string;\n};\n"],["api/_shared/telegram-event-card.ts","const copy = {\n  ru: { open: \"Открыть событие\", calendar: \"В календарь\" },\n  uk: { open: \"Відкрити подію\", calendar: \"У календар\" },\n  cs: { open: \"Otevřít událost\", calendar: \"Do kalendáře\" },\n  en: { open: \"Open event\", calendar: \"Add to calendar\" },\n} as const;\n","const copy = {\n  ru: { open: \"Открыть событие\", calendar: \"В календарь\" },\n  uk: { open: \"Відкрити подію\", calendar: \"У календар\" },\n  cs: { open: \"Otevřít událost\", calendar: \"Do kalendáře\" },\n  en: { open: \"Open event\", calendar: \"Add to calendar\" },\n} as const;\n\nconst beautyCopy = {\n  ru: { open: \"Открыть профиль\" },\n  uk: { open: \"Відкрити профіль\" },\n  cs: { open: \"Otevřít profil\" },\n  en: { open: \"Open profile\" },\n} as const;\n"],["api/_shared/telegram-event-card.ts","export function buildTelegramEventCard(input: TelegramEventCardInput, imageUrl: string) {\n","export function buildTelegramBeautyCard(input: TelegramEventCardInput, imageUrl: string) {\n  const labels = beautyCopy[input.language] || beautyCopy.en;\n  return {\n    type: \"photo\" as const,\n    id: input.eventId.slice(0, 64),\n    photo_url: imageUrl,\n    thumbnail_url: imageUrl,\n    photo_width: 1080,\n    photo_height: 1350,\n    caption: \"\",\n    reply_markup: {\n      inline_keyboard: [[{ text: labels.open, url: input.inviteUrl }]],\n    },\n  };\n}\n\nexport function buildTelegramEventCard(input: TelegramEventCardInput, imageUrl: string) {\n"],["api/_shared/telegram-share-beauty.ts","type PublicBeautyRow = {\n","export type PublicBeautyRow = {\n"],["api/_shared/telegram-share-beauty.ts","export async function loadTrustedTelegramBeautyCard(\n  slug: string,\n  language: ShareLanguage,\n  selectedDate: unknown,\n  _selectedTime: unknown,\n  _publicOrigin: string,\n): Promise<TelegramEventCardInput | null> {\n  void _selectedTime;\n  void _publicOrigin;\n  const client = db();\n  const rows = await loadPublicBeautyRows(client, language);\n  const row = rows.find((item) => item.slug === slug);\n  if (!row) return null;\n\n  const date = normalizeDate(selectedDate, language);\n  const serviceName = localizeBeautyServiceName(row.service_name, language);\n  const city = row.city_id === \"olomouc\" ? \"Olomouc\" : row.city_id;\n\n  return {\n    eventId: row.profile_id,\n    title: serviceName,\n    activity: row.display_name,\n    date: date.display,\n    eventDate: date.raw,\n    time: \"\",\n    address: row.public_location,\n    participants: 0,\n    capacity: 0,\n    icon: \"✨\",\n    inviteUrl: buildTelegramBeautyInviteUrl(row.slug),\n    city,\n    organizer: row.display_name,\n    durationMinutes: row.duration_minutes,\n    price: row.price_czk,\n    level: language === \"cs\" ? \"Beauty služba\" : language === \"en\" ? \"Beauty service\" : language === \"uk\" ? \"Бʼюті-послуга\" : \"Бьюти-услуга\",\n    format: `${row.duration_minutes} min`,\n    environment: row.public_location,\n    isSport: false,\n    language,\n  };\n}\n","const beautyFallbackOrigin = \"https://go-irl-1-0.vercel.app\";\n\nconst buildPublicBeautyProfileUrl = (origin: string, slug: string) => {\n  try {\n    return new URL(`/beauty/${encodeURIComponent(slug)}`, origin || beautyFallbackOrigin).toString();\n  } catch {\n    return new URL(`/beauty/${encodeURIComponent(slug)}`, beautyFallbackOrigin).toString();\n  }\n};\n\nexport function buildTrustedBeautyCardFromRows(\n  rows: PublicBeautyRow[],\n  slug: string,\n  language: ShareLanguage,\n  selectedDate: unknown,\n  publicOrigin: string,\n): TelegramEventCardInput | null {\n  const profileRows = rows.filter((item) => item.slug === slug);\n  const row = profileRows[0];\n  if (!row) return null;\n\n  const date = normalizeDate(selectedDate, language);\n  const services = profileRows\n    .slice(0, 3)\n    .map((item) => ({\n      name: localizeBeautyServiceName(item.service_name, language),\n      priceCzk: item.price_czk,\n    }));\n  const primaryService = services[0] || {\n    name: localizeBeautyServiceName(row.service_name, language),\n    priceCzk: row.price_czk,\n  };\n  const city = row.city_id === \"olomouc\" ? \"Olomouc\" : row.city_id;\n\n  return {\n    eventId: row.profile_id,\n    title: primaryService.name,\n    activity: row.display_name,\n    date: date.display,\n    eventDate: date.raw,\n    time: \"\",\n    address: row.public_location,\n    participants: 0,\n    capacity: 0,\n    icon: \"✨\",\n    inviteUrl: buildTelegramBeautyInviteUrl(row.slug),\n    publicProfileUrl: buildPublicBeautyProfileUrl(publicOrigin, row.slug),\n    beautyServices: services,\n    city,\n    organizer: row.display_name,\n    durationMinutes: row.duration_minutes,\n    price: primaryService.priceCzk,\n    level: language === \"cs\" ? \"Beauty služba\" : language === \"en\" ? \"Beauty service\" : language === \"uk\" ? \"Бʼюті-послуга\" : \"Бьюти-услуга\",\n    format: `${row.duration_minutes} min`,\n    environment: row.public_location,\n    isSport: false,\n    language,\n  };\n}\n\nexport async function loadTrustedTelegramBeautyCard(\n  slug: string,\n  language: ShareLanguage,\n  selectedDate: unknown,\n  _selectedTime: unknown,\n  publicOrigin: string,\n): Promise<TelegramEventCardInput | null> {\n  void _selectedTime;\n  const client = db();\n  const rows = await loadPublicBeautyRows(client, language);\n  return buildTrustedBeautyCardFromRows(rows, slug, language, selectedDate, publicOrigin);\n}\n"],["api/_shared/telegram-share-card-image.ts","import { resolveEventShareBackgroundUrl } from \"./event-share-backgrounds.js\";\n","import { resolveEventShareBackgroundUrl, serviceShareBackgroundUrls } from \"./event-share-backgrounds.js\";\nimport { buildBeautyShareCardSvg } from \"./beauty-share-card-svg.js\";\n"],["api/_shared/telegram-share-card-image.ts","export const renderMetaInvitationCardJpeg = async (input: TelegramEventCardInput) => {\n","export const renderBeautyShareCardJpeg = async (input: TelegramEventCardInput) => {\n  const sharp = await loadSharp();\n  const backgroundUrl = serviceShareBackgroundUrls.manicure;\n  const overlay = { input: Buffer.from(buildBeautyShareCardSvg(input)), left: 0, top: 0 };\n  if (existsSync(backgroundUrl)) {\n    return sharp(readFileSync(backgroundUrl))\n      .resize(1080, 1350, { fit: \"cover\", position: \"attention\" })\n      .composite([overlay])\n      .jpeg({ quality: 90, chromaSubsampling: \"4:4:4\" })\n      .toBuffer();\n  }\n  return sharp({ create: { width: 1080, height: 1350, channels: 3, background: \"#160d1d\" } })\n    .composite([overlay])\n    .jpeg({ quality: 90, chromaSubsampling: \"4:4:4\" })\n    .toBuffer();\n};\n\nexport const renderMetaInvitationCardJpeg = async (input: TelegramEventCardInput) => {\n"],["api/meta/event-preview.ts","import { renderMetaInvitationCardJpeg } from \"../_shared/telegram-share-card-image.js\";\n","import { renderBeautyShareCardJpeg, renderMetaInvitationCardJpeg } from \"../_shared/telegram-share-card-image.js\";\n"],["api/meta/event-preview.ts","const handleBeautyPreview = async (\n","const sendBeautyCardImage = async (card: Parameters<typeof renderBeautyShareCardJpeg>[0], response: VercelResponse) => {\n  const jpeg = await renderBeautyShareCardJpeg(card);\n  setCardImageResponseHeaders(response, jpeg.length);\n  return response.status(200).end(jpeg);\n};\n\nconst handleBeautyPreview = async (\n"],["api/meta/event-preview.ts","  if (format === \"image\" || format === \"download\") {\n    return sendCardImage(card, response, format === \"download\");\n  }\n\n  const query = new URLSearchParams({ slug, language });\n  if (date) query.set(\"date\", date);\n  const canonicalUrl = beautyLandingUrl(origin, slug, language, date);\n  const secret = readEnv(\"META_APP_SECRET\") || readEnv(\"INSTAGRAM_APP_SECRET\");\n  const imageUrl = secret\n    ? `${origin}/api/meta/event-invitation-card?token=${encodeURIComponent(createMetaInvitationCardToken(card, secret))}&v=9`\n    : `${origin}/branding/logo-wide.png`;\n","  if (format === \"image\" || format === \"download\") {\n    return sendBeautyCardImage(card, response);\n  }\n\n  const canonicalUrl = beautyLandingUrl(origin, slug, language, date);\n  const image = new URL(\"/api/meta/event-preview\", origin);\n  image.searchParams.set(\"slug\", slug);\n  image.searchParams.set(\"language\", language);\n  if (date) image.searchParams.set(\"date\", date);\n  image.searchParams.set(\"format\", \"image\");\n  image.searchParams.set(\"v\", \"10\");\n  const imageUrl = image.toString();\n"],["api/meta/event-preview.ts","<meta property=\"og:image:width\" content=\"1080\" /><meta property=\"og:image:height\" content=\"1020\" />","<meta property=\"og:image:width\" content=\"1080\" /><meta property=\"og:image:height\" content=\"1350\" />"],["api/meta/event-preview.ts",".card{max-width:680px;margin:auto;background:#17101f;border:2px solid #d9ad4a;border-radius:24px;overflow:hidden}.hero{width:100%;display:block;aspect-ratio:18/17;object-fit:contain;background:#0a0e10}",".card{max-width:680px;margin:auto;background:#17101f;border:2px solid #d9ad4a;border-radius:24px;overflow:hidden}.hero{width:100%;display:block;aspect-ratio:4/5;object-fit:contain;background:#0a0e10}"],["api/telegram/prepared-beauty-share.ts","import { buildTelegramEventCard } from \"../_shared/telegram-event-card.js\";\n","import { buildTelegramBeautyCard } from \"../_shared/telegram-event-card.js\";\n"],["api/telegram/prepared-beauty-share.ts","import { createTelegramShareCardToken } from \"../_shared/telegram-share-card-token.js\";\n",""],["api/telegram/prepared-beauty-share.ts","    const imageToken = createTelegramShareCardToken(card, botToken);\n    const imageUrl = `${publicOrigin()}/api/telegram/event-share-card?token=${encodeURIComponent(imageToken)}&v=8`;\n","    const image = new URL(\"/api/meta/event-preview\", publicOrigin());\n    image.searchParams.set(\"slug\", body.slug);\n    image.searchParams.set(\"language\", card.language);\n    if (typeof body.date === \"string\" && body.date.trim()) image.searchParams.set(\"date\", body.date.trim());\n    image.searchParams.set(\"format\", \"image\");\n    image.searchParams.set(\"v\", \"10\");\n    const imageUrl = image.toString();\n"],["api/telegram/prepared-beauty-share.ts","        result: buildTelegramEventCard(card, imageUrl),\n","        result: buildTelegramBeautyCard(card, imageUrl),\n"],["src/cardShare.test.ts","  buildOrganicCardShareContent,\n} from \"./cardShare\";\n","  buildOrganicCardShareContent,\n  isBeautyCardShareContent,\n} from \"./cardShare\";\n"],["src/cardShare.test.ts","  it(\"builds a dynamic Beauty preview for WhatsApp without changing Telegram sharing\", () => {\n    const beauty = {\n      title: \"Test Studio\",\n      date: \"03 авг · 09:00\",\n      address: \"Центр, Оломоуц\",\n      url: \"https://go-irl-1-0.vercel.app/beauty/beauty-test-studio\",\n    };\n    const preview = new URL(buildMetaEventPreviewUrl(beauty));\n    expect(preview.pathname).toBe(\"/api/meta/event-preview\");\n    expect(preview.searchParams.get(\"slug\")).toBe(\"beauty-test-studio\");\n    expect(preview.searchParams.get(\"date\")).toBe(beauty.date);\n\n    const download = new URL(buildCardShareDownloadUrl(beauty));\n    expect(download.searchParams.get(\"slug\")).toBe(\"beauty-test-studio\");\n    expect(download.searchParams.get(\"format\")).toBe(\"download\");\n\n    const whatsapp = new URL(buildCardShareTarget(\"whatsapp\", beauty));\n    expect(buildCardShareLandingUrl(beauty)).toBe(\n      \"https://go-irl-1-0.vercel.app/s/beauty-test-studio?date=03+%D0%B0%D0%B2%D0%B3+%C2%B7+09%3A00\",\n    );\n    expect(whatsapp.searchParams.get(\"text\")).toContain(\"https://go-irl-1-0.vercel.app/s/beauty-test-studio\");\n    expect(decodeURIComponent(buildCardShareTarget(\"telegram\", beauty))).toContain(beauty.url);\n  });\n","  it(\"sends Beauty to WhatsApp as exactly one versioned preview URL\", () => {\n    const beauty = {\n      title: \"Test Studio\",\n      date: \"03 авг · 09:00\",\n      address: \"Центр, Оломоуц\",\n      url: \"https://go-irl-1-0.vercel.app/beauty/beauty-test-studio\",\n    };\n    const preview = new URL(buildMetaEventPreviewUrl(beauty));\n    expect(preview.pathname).toBe(\"/api/meta/event-preview\");\n    expect(preview.searchParams.get(\"slug\")).toBe(\"beauty-test-studio\");\n    expect(preview.searchParams.get(\"date\")).toBe(beauty.date);\n    expect(preview.searchParams.get(\"v\")).toBe(\"10\");\n    expect(isBeautyCardShareContent(beauty)).toBe(true);\n\n    const whatsapp = new URL(buildCardShareTarget(\"whatsapp\", beauty));\n    expect(whatsapp.searchParams.get(\"text\")).toBe(preview.toString());\n    expect(whatsapp.searchParams.get(\"text\")).not.toContain(beauty.title);\n    expect(whatsapp.searchParams.get(\"text\")).not.toContain(beauty.address);\n    expect(decodeURIComponent(buildCardShareTarget(\"telegram\", beauty))).toContain(beauty.url);\n  });\n"],["src/components/CardShareAction.whatsapp.ux.test.ts","  it(\"uses Telegram downloadFile without requiring an in-memory File\", () => {\n","  it(\"bypasses the manual JPEG modal for Beauty and opens one WhatsApp preview URL\", () => {\n    const channelClick = source.slice(\n      source.indexOf('if (channel.id === \"whatsapp\")'),\n      source.indexOf(\"</button>\", source.indexOf('if (channel.id === \"whatsapp\")')),\n    );\n    expect(channelClick).toContain(\"canPrepareBeautyTelegramShare(url)\");\n    expect(channelClick).toContain('buildCardShareTarget(\"whatsapp\", content)');\n    expect(channelClick).toContain(\"openExternalShareTarget\");\n    expect(channelClick.indexOf(\"openExternalShareTarget\")).toBeLessThan(channelClick.indexOf(\"void prepareWhatsAppCard()\"));\n  });\n\n  it(\"uses Telegram downloadFile without requiring an in-memory File\", () => {\n"],["api/_shared/telegram-event-card.test.ts","import { buildTelegramEventCard } from \"./telegram-event-card\";\n","import { buildTelegramBeautyCard, buildTelegramEventCard } from \"./telegram-event-card\";\n"],["api/_shared/telegram-event-card.test.ts","  it(\"does not build a calendar action from the localized compact date\", () => {\n","  it(\"builds a Beauty photo with one profile button and no duplicated text\", () => {\n    const imageUrl = \"https://go-irl-1-0.vercel.app/api/meta/event-preview?slug=beauty-test&format=image&v=10\";\n    const result = buildTelegramBeautyCard({\n      ...input,\n      activity: \"Studio Vita\",\n      title: \"Маникюр с гель-лаком\",\n      inviteUrl: \"https://t.me/GOirl_bot?startapp=beauty-test\",\n    }, imageUrl);\n\n    expect(result.type).toBe(\"photo\");\n    expect(result.photo_width).toBe(1080);\n    expect(result.photo_height).toBe(1350);\n    expect(result.caption).toBe(\"\");\n    expect(\"title\" in result).toBe(false);\n    expect(\"description\" in result).toBe(false);\n    expect(result.reply_markup.inline_keyboard).toEqual([[{\n      text: \"Открыть профиль\",\n      url: \"https://t.me/GOirl_bot?startapp=beauty-test\",\n    }]]);\n  });\n\n  it(\"does not build a calendar action from the localized compact date\", () => {\n"],["api/_shared/telegram-share-beauty.test.ts","import { isBeautyShareSlug, loadPublicBeautyRows, localizeBeautyServiceName } from \"./telegram-share-beauty\";\n","import {\n  buildTrustedBeautyCardFromRows,\n  isBeautyShareSlug,\n  loadPublicBeautyRows,\n  localizeBeautyServiceName,\n} from \"./telegram-share-beauty\";\n"],["api/_shared/telegram-share-beauty.test.ts","  it(\"uses the same current public Beauty projection as the Services directory\", async () => {\n","  it(\"builds one trusted Beauty card with up to three service rows\", () => {\n    const base = {\n      profile_id: \"profile-1\",\n      slug: \"beauty-test-studio\",\n      display_name: \"Studio Vita\",\n      city_id: \"olomouc\",\n      public_location: \"Центр, Оломоуц\",\n      duration_minutes: 60,\n      currency: \"CZK\",\n    };\n    const card = buildTrustedBeautyCardFromRows([\n      { ...base, service_name: \"Маникюр с гель-лаком\", price_czk: 890 },\n      { ...base, service_name: \"Педикюр\", price_czk: 990 },\n      { ...base, service_name: \"Nail art\", price_czk: 250 },\n      { ...base, service_name: \"Ignored fourth service\", price_czk: 1 },\n    ], \"beauty-test-studio\", \"ru\", \"2026-08-05\", \"https://go-irl-1-0.vercel.app\");\n\n    expect(card?.activity).toBe(\"Studio Vita\");\n    expect(card?.beautyServices).toEqual([\n      { name: \"Маникюр с гель-лаком\", priceCzk: 890 },\n      { name: \"Педикюр\", priceCzk: 990 },\n      { name: \"Nail art\", priceCzk: 250 },\n    ]);\n    expect(card?.publicProfileUrl).toBe(\"https://go-irl-1-0.vercel.app/beauty/beauty-test-studio\");\n  });\n\n  it(\"uses the same current public Beauty projection as the Services directory\", async () => {\n"],["tests/api/meta/event-preview.test.ts","  it(\"routes both Activity and Service download formats through attachment mode\", () => {\n    expect(source.match(/format === \"image\" \\|\\| format === \"download\"/g)).toHaveLength(2);\n    expect(source.match(/format === \"download\"/g)?.length).toBeGreaterThanOrEqual(4);\n  });\n","  it(\"routes Activity downloads through attachment mode and Beauty through the canonical renderer\", () => {\n    expect(source.match(/format === \"image\" \\|\\| format === \"download\"/g)).toHaveLength(2);\n    expect(source).toContain(\"renderBeautyShareCardJpeg\");\n    expect(source).toContain('image.searchParams.set(\"v\", \"10\")');\n    expect(source).toContain('og:image:height\" content=\"1350\"');\n  });\n"]];
const writes = [["api/_shared/beauty-share-card-svg.ts","import type { TelegramEventCardInput } from \"./telegram-event-card.js\";\n\nconst copy = {\n  ru: { cta: \"Услуги и запись\", priceFrom: \"от\", slogan: \"МЕНЬШЕ СКРОЛЛИНГА. БОЛЬШЕ ЖИЗНИ.\" },\n  uk: { cta: \"Послуги та запис\", priceFrom: \"від\", slogan: \"МЕНШЕ СКРОЛІНГУ. БІЛЬШЕ ЖИТТЯ.\" },\n  cs: { cta: \"Služby a rezervace\", priceFrom: \"od\", slogan: \"MÉNĚ SCROLLOVÁNÍ. VÍCE ŽIVOTA.\" },\n  en: { cta: \"Services and booking\", priceFrom: \"from\", slogan: \"LESS SCROLLING. MORE LIFE.\" },\n} as const;\n\nconst xml = (value: string) => value\n  .replaceAll(\"&\", \"&amp;\")\n  .replaceAll(\"<\", \"&lt;\")\n  .replaceAll(\">\", \"&gt;\")\n  .replaceAll('\"', \"&quot;\")\n  .replaceAll(\"'\", \"&apos;\");\n\nconst clean = (value: string, max = 140) => value.trim().replace(/\\s+/g, \" \").slice(0, max);\n\nconst wrap = (value: string, maxChars: number, maxLines = 2) => {\n  const words = clean(value).split(\" \").filter(Boolean);\n  const lines: string[] = [];\n  for (const word of words) {\n    const current = lines.at(-1) || \"\";\n    if (!current || `${current} ${word}`.length <= maxChars) {\n      if (current) lines[lines.length - 1] = `${current} ${word}`;\n      else lines.push(word);\n    } else if (lines.length < maxLines) {\n      lines.push(word);\n    } else {\n      lines[lines.length - 1] = `${lines.at(-1) || \"\"}…`.slice(0, maxChars);\n      break;\n    }\n  }\n  return lines.slice(0, maxLines);\n};\n\nconst textLines = (lines: string[], x: number, y: number, lineHeight: number) =>\n  lines.map((line, index) => `<tspan x=\"${x}\" y=\"${y + index * lineHeight}\">${xml(line)}</tspan>`).join(\"\");\n\nconst compactProfileLink = (value: string) => {\n  try {\n    const url = new URL(value);\n    return clean(`${url.hostname}${url.pathname}`, 70).replace(/\\/$/, \"\");\n  } catch {\n    return clean(value, 70);\n  }\n};\n\nexport const buildBeautyShareCardSvg = (input: TelegramEventCardInput) => {\n  const labels = copy[input.language] || copy.en;\n  const name = clean(input.activity || input.organizer || \"GO IRL Beauty\", 80);\n  const subtitle = clean(input.level || input.title, 90);\n  const initial = name.slice(0, 1).toUpperCase() || \"G\";\n  const services = (input.beautyServices?.length\n    ? input.beautyServices\n    : [{ name: input.title, priceCzk: input.price }])\n    .filter((service) => clean(service.name))\n    .slice(0, 3);\n  const serviceRows = services.map((service, index) => {\n    const y = 660 + index * 112;\n    const serviceName = clean(service.name, 70);\n    return `<g data-beauty-service-row=\"${index + 1}\">\n      <rect x=\"74\" y=\"${y}\" width=\"932\" height=\"90\" rx=\"24\" fill=\"#24182b\" fill-opacity=\".82\" stroke=\"#e2bd66\" stroke-opacity=\".46\" stroke-width=\"2\"/>\n      <text x=\"104\" y=\"${y + 57}\" fill=\"#fff7fb\" font-size=\"32\" font-weight=\"750\">${xml(serviceName)}</text>\n      <text x=\"976\" y=\"${y + 57}\" text-anchor=\"end\" fill=\"#f1cb72\" font-size=\"31\" font-weight=\"850\">${xml(`${labels.priceFrom} ${Math.round(service.priceCzk)} Kč`)}</text>\n    </g>`;\n  }).join(\"\");\n  const location = clean(input.address || input.city, 80);\n  const profileLink = compactProfileLink(input.publicProfileUrl || input.inviteUrl);\n\n  return `<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"1080\" height=\"1350\" viewBox=\"0 0 1080 1350\">\n  <defs>\n    <linearGradient id=\"beautyTop\" x1=\"0\" y1=\"0\" x2=\"0\" y2=\"1\"><stop stop-color=\"#0d0812\" stop-opacity=\".78\"/><stop offset=\"1\" stop-color=\"#0d0812\" stop-opacity=\"0\"/></linearGradient>\n    <linearGradient id=\"beautyShade\" x1=\"0\" y1=\"0\" x2=\"0\" y2=\"1\"><stop stop-color=\"#120a18\" stop-opacity=\".04\"/><stop offset=\".38\" stop-color=\"#120a18\" stop-opacity=\".72\"/><stop offset=\"1\" stop-color=\"#0a060e\" stop-opacity=\".98\"/></linearGradient>\n    <linearGradient id=\"beautyLogo\" x1=\"0\" y1=\"0\" x2=\"1\" y2=\"1\"><stop stop-color=\"#e2bd66\"/><stop offset=\"1\" stop-color=\"#9f73c8\"/></linearGradient>\n  </defs>\n  <rect width=\"1080\" height=\"540\" fill=\"url(#beautyTop)\"/>\n  <rect y=\"300\" width=\"1080\" height=\"1050\" fill=\"url(#beautyShade)\"/>\n  <rect x=\"28\" y=\"28\" width=\"1024\" height=\"1294\" rx=\"46\" fill=\"none\" stroke=\"#e0bc65\" stroke-opacity=\".92\" stroke-width=\"3\"/>\n  <g font-family=\"DejaVu Sans, Arial, sans-serif\">\n    <text x=\"74\" y=\"102\" fill=\"#e2bd66\" font-size=\"26\" font-weight=\"800\" letter-spacing=\"5\">GO IRL BEAUTY</text>\n    <rect x=\"74\" y=\"142\" width=\"150\" height=\"150\" rx=\"38\" fill=\"url(#beautyLogo)\" stroke=\"#fff4da\" stroke-opacity=\".8\" stroke-width=\"3\"/>\n    <text x=\"149\" y=\"244\" text-anchor=\"middle\" fill=\"#1a1020\" font-size=\"72\" font-weight=\"900\">${xml(initial)}</text>\n    <text fill=\"#fff9fb\" font-size=\"72\" font-weight=\"900\">${textLines(wrap(name, 24, 2), 74, 520, 76)}</text>\n    <text x=\"76\" y=\"620\" fill=\"#e7dce9\" font-size=\"38\" font-weight=\"600\">${xml(subtitle)}</text>\n    ${serviceRows}\n    <text x=\"76\" y=\"1080\" fill=\"#d9cddd\" font-size=\"31\" font-weight=\"650\">⌖ ${xml(location)}</text>\n    <rect x=\"74\" y=\"1130\" width=\"932\" height=\"96\" rx=\"26\" fill=\"#e2bd66\"/>\n    <text x=\"112\" y=\"1191\" fill=\"#1b111f\" font-size=\"34\" font-weight=\"900\">${xml(labels.cta)}</text>\n    <text x=\"966\" y=\"1191\" text-anchor=\"end\" fill=\"#1b111f\" font-size=\"42\" font-weight=\"900\">→</text>\n    <text x=\"76\" y=\"1282\" fill=\"#d4c7d6\" font-size=\"25\" font-weight=\"650\">${xml(profileLink)}</text>\n    <text x=\"1004\" y=\"1282\" text-anchor=\"end\" fill=\"#e2bd66\" font-size=\"23\" font-weight=\"800\">${xml(labels.slogan)}</text>\n  </g>\n</svg>`;\n};\n"],["api/_shared/beauty-share-card-svg.test.ts","import { describe, expect, it } from \"vitest\";\nimport { buildBeautyShareCardSvg } from \"./beauty-share-card-svg\";\n\ndescribe(\"Beauty share card SVG\", () => {\n  it(\"renders the canonical 1080x1350 business card with three services\", () => {\n    const svg = buildBeautyShareCardSvg({\n      eventId: \"profile-1\",\n      title: \"Маникюр\",\n      activity: \"Studio Vita\",\n      date: \"\",\n      eventDate: \"\",\n      time: \"\",\n      address: \"Центр, Оломоуц\",\n      participants: 0,\n      capacity: 0,\n      icon: \"✨\",\n      inviteUrl: \"https://t.me/GOirl_bot?startapp=beauty-test\",\n      publicProfileUrl: \"https://go-irl-1-0.vercel.app/beauty/beauty-test\",\n      city: \"Olomouc\",\n      price: 890,\n      level: \"Бьюти-услуга\",\n      format: \"60 min\",\n      environment: \"Центр, Оломоуц\",\n      language: \"ru\",\n      beautyServices: [\n        { name: \"Маникюр\", priceCzk: 890 },\n        { name: \"Педикюр\", priceCzk: 990 },\n        { name: \"Nail art\", priceCzk: 250 },\n      ],\n    });\n\n    expect(svg).toContain('width=\"1080\" height=\"1350\"');\n    expect(svg.match(/data-beauty-service-row=/g)).toHaveLength(3);\n    expect(svg).toContain(\"GO IRL BEAUTY\");\n    expect(svg).toContain(\"Услуги и запись\");\n    expect(svg).toContain(\"go-irl-1-0.vercel.app/beauty/beauty-test\");\n  });\n});\n"]];
for (const [file, before, after] of replacements) replaceOne(file, before, after);
for (const [file, content] of writes) write(file, content);
console.log("BEAUTY_SHARE_CANONICAL_PATCH_APPLIED");
